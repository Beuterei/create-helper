on:
  workflow_dispatch:
  workflow_call:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  testing:
    uses: beuluis/reusable-workflows/.github/workflows/qa-node-testing.yml@main

