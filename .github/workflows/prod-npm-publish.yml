name: 'Publish pack with latest dist-tag'

on:
  release:
    types:
      - created

jobs:
  testing:
    uses: ./.github/workflows/qa-check.yml

  publish-npm:
    needs: testing
    uses: beuluis/reusable-workflows/.github/workflows/publish-npm.yml@main
    secrets: inherit
